import { UnitValuePipe } from './unit-value.pipe';

describe('UnitValuePipe', () => {
  it('create an instance', () => {
    const pipe = new UnitValuePipe();
    expect(pipe).toBeTruthy();
  });
});
